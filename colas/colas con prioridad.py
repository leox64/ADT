"""
Colas de prioridad: Es una vesión extendida de la implementación de cola básica, pero con la
variante de cada elemento insetado llevará un número que indica una prioridad especifica,
los elmentos son ordenados en base a esa prioridad. Si hay más de un elemento con la misma
prioridad estos s e menipulan con el protocolo "fifo".
Cola doble: una cola doble es aquella en la que se puede agregar tanto al final como al
inicio, de la misma forma se puedan sacar de principio y del final. El ADT es el siguiente:
DoubleQueue()
add_first(value)
add_last(value)
dequeue_first()
dequeue_last()
first()
last()
is_empty()
length()
to_string()

"""
## colas dobles
