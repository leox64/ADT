"""
Cola: Una cola es una estructura de datos lineal  que almacena datos manupulados con el
protocolo fifo (first-in,first-out). Esto significa que los elmentos son insetados al
final (enqueue) de la estructura, elminando elementos qiue se encuantran en la cabeza
de la estructrura (dequeue).
La estructrura cola tiene la siguientes operaciones: El constructor queue(), la función is_empty() que
averigua si la cola está vacía, el método length() regresa el numero de elementos en la cola,
el metodo enqueue()que recibe un elemento y lo agrega al final de la cola y el metodo dequeue()
que elminia el elemento al principio de la cola y lo regresa.

"""
